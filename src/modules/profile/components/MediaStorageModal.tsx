import MediaStorage from './MediaStorage';

interface MediaStorageModalProps {
  userId: string;
  isOpen: boolean;
  onClose: () => void;
}

export default function MediaStorageModal({ userId, isOpen, onClose }: MediaStorageModalProps) {
  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
      <div className="bg-white rounded-2xl shadow-2xl w-full max-w-6xl max-h-[90vh] overflow-hidden">
        {/* Header */}
        <div className="flex items-center justify-between p-6 border-b">
          <div>
            <h2 className="text-2xl font-bold text-gray-900">Kho lưu trữ Media</h2>
            <p className="text-gray-500">Quản lý ảnh, video và tài liệu của bạn</p>
          </div>
          <button
            onClick={onClose}
            className="p-2 hover:bg-gray-100 rounded-full transition-colors"
          >
            <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
        
        {/* Content */}
        <div className="p-6 overflow-y-auto max-h-[calc(90vh-120px)]">
          <MediaStorage userId={userId} />
        </div>
      </div>
    </div>
  );
}
